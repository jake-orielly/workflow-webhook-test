name: webhook-trigger

on:
  repository_dispatch:
    types: 
      - webhook-one

jobs:
  build:
    name: build
    steps:
    - name: Hello
      run: echo "Hello, I've been triggered by a webhook!!"