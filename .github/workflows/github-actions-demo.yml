name: GitHub Actions Demo
on:
  repository_dispatch:
    types: 
      - webhook-one
      - webhook-two

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
    - name: Hello
      run: echo "Hello, I've been triggered by a webhook!"