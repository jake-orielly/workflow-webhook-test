name: GitHub Actions Demo
on:
  repository_dispatch:
    types: 
      - webhook-one
jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
    - name: Hello
      run: echo "Hello, I've been triggered by a webhook!"